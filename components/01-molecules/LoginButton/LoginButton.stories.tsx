import { Meta } from '@storybook/react'

import LoginButton from '.'

export default {
  title: 'Molecules/LoginButton',
  component: LoginButton
} as Meta

export const DefaultLoginButton = () => <LoginButton />
